{% extends 'base.html.twig' %}

{% block title %}Edit Exchange{% endblock %}

{% block body %}
    <h1>Edit Exchange</h1>

    {{ include('exchange/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('exchange_index') }}">back to list</a>

    {{ include('exchange/_delete_form.html.twig') }}
{% endblock %}
