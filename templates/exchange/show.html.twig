{% extends 'base.html.twig' %}

{% block title %}Exchange{% endblock %}

{% block body %}
    <h1>Exchange</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exchange.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ exchange.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('exchange_index') }}">back to list</a>

    <a href="{{ path('exchange_edit', {'id': exchange.id}) }}">edit</a>

    {{ include('exchange/_delete_form.html.twig') }}
{% endblock %}
