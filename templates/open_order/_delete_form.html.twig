<form method="post" action="{{ path('open_order_delete', {'id': open_order.id}) }}" onsubmit="confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ open_order.id) }}">
    <button class="btn">Delete</button>
</form>
