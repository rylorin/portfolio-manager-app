{% extends 'base.html.twig' %}

{% block title %}Edit {{ type }} Position {{ position.id }}{% endblock %}

{% block body %}
  <div class="row">
    {% set item_size='col-12 col-sm-6' %}
  	<div class="{{ item_size }}">
        {{ include('position/_form.html.twig', {'button_label': 'Update'}) }}
        <a href="{{ path('portfolio_position_show', {'id': position.id}) }}"><span data-feather="arrow-left"></span></a>
        <a href="#" onclick="click_button('save')"><span data-feather="save"></span></a>
        {{ include('position/_delete_form.html.twig') }}
  	</div>
  </div>
{% endblock %}
